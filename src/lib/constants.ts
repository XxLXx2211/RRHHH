
import { Sexo, CanalRecepcion, TipoContacto, EstatusCandidato, AreaInteres } from '@/types';

export const SEXO_OPTIONS = Object.values(Sexo);
export const CANAL_RECEPCION_OPTIONS = Object.values(CanalRecepcion);
export const TIPO_CONTACTO_OPTIONS = Object.values(TipoContacto);
export const ESTATUS_CANDIDATO_OPTIONS = Object.values(EstatusCandidato);
export const AREA_INTERES_OPTIONS = Object.values(AreaInteres);

export const BOOLEAN_OPTIONS = ["Sí", "No"];

export const EDAD_FILTER_OPTIONS = [
  "18-25",
  "26-30",
  "31-35",
  "36-40",
  "41-50",
  "51+",
  "Todos",
];

